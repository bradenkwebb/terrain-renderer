#include <iostream>
#include <windows.h>
#include <glad/glad.h>
#include <GLFW/glfw3.h>

int main() {
    // if (!glfwInit()) {
    //     std::cerr << "Failed to initialize GLFW" << std::endl;
    //     return -1;
    // }
    int numSecs = 5;
    // Sleep(numSecs);

    std::cout << "Hello World!" << std::endl;
    std::cout << __cplusplus << std::endl;
    // std::cout << std::format("Sleeping for {} seconds", str(numSecs)) << std::endl;
    return 0;
}